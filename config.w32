// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("crontab", "for crontab support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("crontab", "enable crontab support", "no");

if (PHP_CRONTAB != "no") {
	EXTENSION("crontab", "crontab.c");
}

